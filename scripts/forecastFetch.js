import {cityDatabase} from './citiesDatabase'

window.onload = () =>{

    // fetch the data from the endpoint 
    // turn that data into a json object and create a data function 

    //when a city is selected i want the log and lat 

    //than i want to find the log and lat in the api 

    //open the api data and displace
   fetch()
    if(cityDatabase.longitude,cityDatabase.latitude ==  selectedCity.latitude, selectedCity.longitude){
        let stationLookupUrl =`https://api.weather.gov/points/${selectedCity.latitude},${selectedCity.longitude}`;
    }
    
}